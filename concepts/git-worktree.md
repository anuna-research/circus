# Git worktree

A Git worktree gives one worker a separate checkout and branch without cloning
the whole repository. Circus prepares one per task attempt to isolate edits and
preserve the result for review.
