# hence

`hence` is the predecessor that combined planning, allocation, agent execution,
evaluation, and merging under one supervisor. Circus takes only the local
worktree, terminal, completion, and hand-off concerns from that shape.
